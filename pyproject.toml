[project]
name = "recon-harness"
version = "0.2.0"
description = "General web recon with full collection and prioritized normalized routes"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
recon-harness = "recon_harness.cli:main"

[tool.setuptools]
package-dir = {"" = "code"}
py-modules = ["dorkgen"]

[tool.setuptools.packages.find]
where = ["code"]

[tool.unittest]
start-directory = "tests"
