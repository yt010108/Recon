[project]
name = "recon-harness"
version = "0.2.0"
description = "Policy-gated competition web recon with normalized routes and prioritized candidates"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
recon-harness = "recon_harness.cli:main"

[tool.setuptools]
package-dir = {"" = "code"}

[tool.setuptools.packages.find]
where = ["code"]

[tool.unittest]
start-directory = "tests"
