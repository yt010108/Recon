#!/bin/sh
set -eu

repo_root="$(git rev-parse --show-toplevel)"
guard="$repo_root/scripts/check-secrets.py"
if [ ! -f "$guard" ]; then
  guard="$repo_root/recon-harness/scripts/check-secrets.py"
fi

if command -v py >/dev/null 2>&1; then
  exec py -3 "$guard"
fi
exec python3 "$guard"
